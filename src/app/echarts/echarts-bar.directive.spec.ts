import { EchartsBarDirective } from './echarts-bar.directive';

describe('EchartsBarDirective', () => {
  it('should create an instance', () => {
    const directive = new EchartsBarDirective();
    expect(directive).toBeTruthy();
  });
});
